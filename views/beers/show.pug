extends ../layouts/main.pug
block content
  .container
    header
      h1= beer.name

    div
      br/
      h5= "Tester: " + beer.tester.firstName
      h5= "Origin: " + beer.origin
      h5= "Brewery: " + beer.brewery
      br/
      p= "Description: " + beer.description


      a(href='/beers') Show all the beer